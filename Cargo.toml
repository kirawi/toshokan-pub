[package]
name = "toshokan"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.5"
tokio = { version = "1", features = ["full"] }
# serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }

tracing = "0.1"
tracing-subscriber = "0.3"

anyhow = "1.0"
